# QuakeStatTelegramBot
Hot to build:
```
mvn clean compile assembly:single
```

How to run:
```
java -cp QuakeStatBotM-1.0-SNAPSHOT-jar-with-dependencies.jar quake.alko.bot.Runner PATH_TO_BOT_CONFIG
```
* see bot.config.sample
